# Скопируй в .env и подставь свой токен:  cp .env.example .env
# .env в .gitignore — в репозиторий не попадёт. Сюда РЕАЛЬНЫЙ токен НЕ вписывай!
CODEAPI_BASE=https://mail.veximail.xyz:8443
CODEAPI_TOKEN=PUT_YOUR_TOKEN_HERE
